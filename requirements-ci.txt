# Minimal CI requirements — all heavy ML/AI/data libs are stubbed in
# tests/conftest.py via sys.modules, so they don't need to be installed.
#
# Every package here is pure Python or ships pre-built binary wheels —
# NONE of them can transitively pull pycairo or any other C library build.

# Django stack (auto-pulls: asgiref, sqlparse)
Django==5.1.6
djangorestframework==3.16.1
django-cors-headers==4.9.0
dj-database-url==2.1.0
python-dotenv==1.0.0
psycopg[binary]>=3.1

# Testing
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
flake8==7.1.1
